import { Swiper, SwiperSlide } from 'swiper/react';
import MainSwiper from '@/components/main/MainSwiper';
const Home = () => {
  return (
    <main className=''>
      <MainSwiper />
    </main>
  );
};

export default Home;
